{
  "name": "@cacheline/dom",
  "version": "0.1.0",
  "description": "W3C-compatible DOM backed by flat array pool with generational handles",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run",
    "bench": "vitest bench"
  },
  "devDependencies": {
    "jsdom": "^29.0.1",
    "tsup": "~8.0.0",
    "typescript": "~5.5.0",
    "vitest": "~3.0.0"
  }
}
